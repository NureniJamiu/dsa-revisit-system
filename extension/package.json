{
  "name": "restack-extension",
  "version": "0.1.0",
  "private": true,
  "description": "ReStack Chrome extension: one-click add-to-queue from LeetCode, GFG, HackerRank, NeetCode.",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.5.4",
    "@types/chrome": "^0.0.270"
  }
}
